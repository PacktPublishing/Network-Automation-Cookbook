- change generate data playbook to use blockinfile with template lookup for jinja2 files.
- How to model single metrics in grafana

- test multiple l3vpns
- how to do replace for l3vpns
- change the transformation playbook to use blockinfile

#### Bug ###
- had to change ios_xr.py config to use str not byte

from: >> file = open(src, 'wb')
TO: >> file = open(src, 'w')
Need to find out why work with not replace
and why not work with replace


